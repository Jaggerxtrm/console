// layoutGraph was deleted in forge-2a8a.2; React Flow + dagre owns layout now.
// New tests for buildFlowGraph land in forge-2a8a.4 cleanup.

import { describe, it } from "vitest";

describe("graph layout (replaced by dagre via React Flow)", () => {
  it.skip("rewritten in forge-2a8a.4 cleanup", () => {});
});

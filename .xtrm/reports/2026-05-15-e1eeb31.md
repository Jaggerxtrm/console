---
session_date: 2026-05-15
branch: feat/forge-releases-tab
commits: 14
issues_closed: 17
issues_filed: 18
specialist_dispatches: 4
models_used:
  - openai-codex/gpt-5.4-mini
  - openai-codex/gpt-5.3-codex
  - openai-codex/gpt-5.5
---

# Session Report — 2026-05-15

## Summary

Shipped PR #7 (forge-density-and-bg-revert merge to main) early in the session, then opened PR #8 `feat/forge-releases-tab` as the umbrella branch for everything else: a Releases tab integrated into gitboard alongside Activity/PRs/Issues, a /api/github/repo/{owner}/{name}/{markdown,reports,reports/:file} backend with 10-min cache + YAML-frontmatter parsing, README / CHANGELOG / Reports tabs in gitboard's TabBar (no separate /console app — earlier scaffold attempt was reverted as architectural regression), beadboard kanban click-to-overlay using the existing IssueDossier in a portal modal with focus trap, beadboard Triage panel rewritten to reuse the Feed IssueRow primitive, client-side PR auto-link badges on beadboard issues matched by id-in-PR-title/body, and a consistency pass on gitboard detail panels (shared `.gb-detail-stack` clamp 560/75%/1400 centered, `.pr-body-text`/`.pr-rich-text` reused everywhere with **bold**/*italic* + fenced code + tables, surface-secondary background on README/CHANGELOG/Reports pane with accent rail). Closed two open Codex review findings on PR #8 (the global-sort-releases fix landed; the tag_name vs name schema split deferred to forge-6q1). Filed but split forge-yhm into forge-f0g (GH live poll + WS push, 1-min cadence for active repos, ETag + watermarks) and forge-igg (Dolt commit-hash + fs.watch fallback for beads live updates) with full v3 contracts after two overthinker passes converged on the design.

## Issues Closed (17)

### Releases + dossier markdown shipping
| ID | Title | Type | Specialist | Close Reason |
|----|-------|------|------------|--------------|
| forge-uqp | Releases tab in gitboard (integrate alongside Activity/PRs/Issues) | feature | executor 663dfa (partial → manual fix) | New `ReleaseTimeline.tsx` + `/api/github/releases` + Tab entry + store slice. Reused `renderPrBodyText` + virtualizer pattern from PrTimeline. |
| forge-xdi | ReleaseTimeline dossier: reuse pr-body-text class so markdown renders | bug | manual | Swap `.release-body-text` → `.pr-body-text` + `.pr-rich-text` |
| forge-xdx | renderPrBodyText: add bold/italic + visible h3 | bug | manual | Regex extended for `**bold**` + `*italic*`; `.pr-rich-text` descendant CSS (h3, strong, em, code) |
| forge-e6h | ReleaseTimeline: render full body by default (drop truncate) | task | manual | Removed `truncateBody` + show-more state |

### Beadboard integration
| ID | Title | Type | Close Reason |
|----|-------|------|--------------|
| forge-buv | Kanban: shrink chips, expand-on-click via overlay | task | `IssueOverlay` (portal + focus trap + body-scroll lock) wraps exported `IssueDossier`; KanbanBoard owns single `selectedId` |
| forge-8xj | Triage: render as Feed-style rows ordered by triage | task | Triage now renders Top picks/Quick wins/Stale via shared `IssueRow`; expand inline matches Feed |
| forge-1kv | PR auto-linking from worktrees and commits | feature | Client-side: regex match issue.id (escape dots) against open-PR title+body; badge on IssueRow + BeadCard; no schema/poller changes |
| forge-53d | Fix segmentCounts key mismatch in beadboard App | bug | Earlier in day: 1-char key rename for Codex P2 on PR #7 |

### RepoDossier → Tabs (architectural correction)
| ID | Title | Type | Close Reason |
|----|-------|------|--------------|
| forge-xnk | RepoDossier overlay in gitboard (README/CHANGELOG/Reports) | feature | First implementation as modal overlay; preserved for beadboard kanban reuse |
| forge-5rr | Repo content as tabs (not modal) + fenced code + table rendering | bug | User correction: dropped modal in gitboard, moved README/CHANGELOG/Reports into TabBar; extracted `RepoContentPanels.tsx`; renderPrBodyText now parses fenced code + markdown tables |

### Consistency / visual fixes
| ID | Title | Type | Close Reason |
|----|-------|------|--------------|
| forge-cw6 | Gitboard consistency pass | bug | `.gb-detail-stack` clamps PR/Issue/Release/README/CHANGELOG/Reports to 75% center; IssueExpandedBody uses `.pr-body-text` + `.pr-rich-text`; Reports endpoint parses YAML frontmatter |
| forge-z6t | README/CHANGELOG/Reports tab content: center align with PR expanded view + softer code bg | bug | `.gb-tab-pane` wrapper mirrors `.pr-expanded-body` padding; rich-code bg → transparent |
| forge-hyl | Release row grid mis-aligned + duplicate tag/title cells | bug | Drop redundant `.release-tag` span; grid template 6 columns matches 6 cells |
| forge-otj | Detail panel centering: strip asymmetric padding-left | bug | Removed `padding-left: 90/118px` from all expanded body classes; stack centers symmetrically (l=138, r=138 verified via Playwright) |
| forge-z5k | README/CHANGELOG/Reports: add card-like background | bug | `.gb-tab-pane` now has `var(--surface-secondary)` + 2px accent rail |
| forge-w8s | Codex P2: sort release list globally by published_at | bug | `.flatMap(...).sort((a,b) => b.published_at.localeCompare(a.published_at))` |

### Scope decisions (closed without code)
| ID | Title | Type | Close Reason |
|----|-------|------|--------------|
| forge-yhm | WebSocket: live new_pr and new_issue push events | feature | Superseded by forge-f0g (GH live) + forge-igg (beads live) split after two overthinker passes |
| forge-dsz / forge-7ff | SCOPE: forge-yhm live updates (parallel overthinker beads) | decision | Both verdicts captured → produced forge-f0g + forge-igg contracts |
| forge-gg4 / forge-c0k / forge-gko / forge-v9h | Earlier /console-era specs | feature | Superseded by integrated tabs/dossier work (forge-uqp / forge-xnk / forge-5rr) |
| forge-z6j / forge-z6j.2 | /console epic + child | task/epic | Abandoned: /console scaffold approach regressed design; replaced by integration |

## Issues Filed (18)

| ID | P | Type | Title | Why |
|----|---|------|-------|-----|
| forge-uqp | P1 | feature | Releases tab in gitboard | First post-/console-revert; explicit "no parallel primitives" reuse rules in CONSTRAINTS |
| forge-xdi | P2 | bug | ReleaseTimeline dossier reuse pr-body-text class | User: markdown not rendering; root cause is unscoped `.release-body-text` class |
| forge-xdx | P2 | bug | renderPrBodyText bold/italic + visible h3 | User-visible literal `**` and h3 indistinguishable from body |
| forge-e6h | P3 | task | Render full release body by default | User pref: no truncation |
| forge-xnk | P2 | feature | RepoDossier overlay (README/CHANGELOG/Reports) | First take: modal; later corrected by forge-5rr to tabs |
| forge-5rr | P1 | bug | Repo content as tabs not modal + fenced code + table | User correction after the modal landed; expanded scope to add markdown-table + ```fenced``` rendering |
| forge-buv (re-opened context) | P2 | task | Beadboard Kanban overlay | User explicit: keep dossier modal but move to beadboard kanban |
| forge-8xj | P2 | task | Triage as Feed rows | Visual consistency with Feed |
| forge-1kv (re-opened) | P2 | feature | PR auto-link | User reported missing collaborator issue; broader story |
| forge-cw6 | P1 | bug | Gitboard consistency pass | User: "everything inconsistent — width, markdown, reports plain" |
| forge-z6t | P2 | bug | README/CHANGELOG center align + softer code bg | User: not centered, bg too dark |
| forge-hyl | P1 | bug | Release row grid mis-aligned | User screenshot: title wrapping to 4 lines, empty right side |
| forge-otj | P1 | bug | Strip asymmetric padding-left | User: "every detail central is skewed right" |
| forge-z5k | P3 | bug | README/CHANGELOG card background | User: "no nice bg like the others" |
| forge-dsz | P2 | decision | Scope forge-yhm live updates (overthinker 1) | Pre-implementation design |
| forge-7ff | P2 | decision | Scope (parallel overthinker 2) | Get two independent perspectives in parallel |
| forge-f0g | P2 | feature | Live GH PR/Issue updates (yhm.1) | Split per overthinker recommendation; full v3 contract with imperative reuse |
| forge-igg | P2 | feature | Live Beads updates (yhm.2) | Dolt commit-hash poll + fs.watch fallback; depends on forge-f0g |
| forge-w8s | P2 | bug | Codex P2 release sort | Codex review on PR #8 |
| forge-6q1 | P3 | bug | Codex P2 tag_name vs name schema split | Deferred; needs schema migration |

## Specialist Dispatches

| Wave | Specialists | Model | Bead(s) | Outcome |
|------|-------------|-------|---------|---------|
| 1 | executor (job 663dfa) | gpt-5.4-mini | forge-uqp | Partial: tests/lint reported false-positive due to worktree-deps; merged after manual review (small diff, 411 LOC) |
| 2 | overthinker (job 2bee39) | gpt-5.5 | forge-dsz | DONE — produced detailed Q1..Q8 scoping doc; recommended split |
| 2 | overthinker (job 7f4eed) | gpt-5.5 | forge-7ff (clone) | DONE in parallel — converged with #1 on all Qs except channel arch (one unified vs two separate channels) |

### Specialist problems
| Problem | Bead | Notes |
|---------|------|-------|
| Worktree deps not installed → executor lint/test marked false fail | forge-uqp | After running `bun install` in worktree, tests passed. Manual completion after stop. |
| `sp run overthinker --bead <same>` twice silently took only first dispatch | forge-dsz | Worked around by cloning bead to forge-7ff |

## Problems Encountered

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| Friend's GitHub issue not appearing in gitboard | Poller polls /users/{me}/events only — collaborators' issues never enter the events feed; `backfillPrsAndIssues` runs once at startup, never re-fetches per-repo /issues | One-off backfill script via gh CLI + UPSERT into github_issues; periodic backfill scoped into forge-f0g |
| Body content missing on existing ReleaseEvent rows after adding `body` to poller | INSERT-OR-IGNORE on duplicate id prevents UPSERT; existing rows never get body populated | One-off backfill: `gh api /repos/{repo}/releases --paginate` + UPDATE github_events SET body=... matching by html_url |
| Localhost showed nothing on `localhost:3010/console` even though `127.0.0.1:3010` worked | Two bun processes: orphan dev server bound to IPv6 `::1`; new prod server on IPv4 `0.0.0.0`. Browser's `localhost` resolved to ::1 first → hit the wrong process | Killed orphan; happy-eyeballs fell back to IPv4 |
| Reviewer specialist refused to publish PASS in structured format on resume turn | Reviewer returned plain text "PASS." instead of full Compliance Verdict block | Manual merge via `git merge --no-ff` (already documented as known limitation in memory `sp-merge-fails-with-bookkeeping-conflicts-when-the`) |
| Markdown body invisible despite valid HTML output | `.release-body-text` had no descendant styles; browser defaults insufficient on dark bg + tiny font | Switched to `.pr-body-text` + `.pr-rich-text` shared with PrTimeline (forge-xdi); later added bold/italic + h3 styles (forge-xdx) |
| `release-row-main` grid had 5 columns but 7 child spans | Cargo-cult from initial /console scaffold; never reconciled when row layout grew | Drop redundant `.release-tag` span (poller stores same value in tag_name and name anyway); 6 columns now match 6 cells (forge-hyl) |
| Detail panels skewed right of true center | Inherited `padding-left: 90px` on `.pr-expanded-body` (meant to align with row's icon offset) propagated to all expanded variants and to `.gb-tab-pane` — centered stack drifted | Stripped padding-left from all 4 expanded-body classes + gb-tab-pane (forge-otj); verified l=138, r=138 symmetric via Playwright |
| `/console` scaffold visually regressed the apps | Executors built parallel primitives despite "reuse RepoSidebar/IssueFeed/PrTimeline" in CONSTRAINTS prose | Reverted via `git reset --hard main`; relearned: bead constraints must say "IMPORT X from Y; do not write a replacement" imperatively. Memory `when-briefing-executor-for-additions-to-existing-apps` saved. |

## Code Changes

### Change summary
- Added: 7 files
- Modified: 25 files (final stack on PR #8)
- 14 commits since main

### New files
- `apps/gitboard/src/dashboard/components/github/ReleaseTimeline.tsx` — virtualized timeline, day-grouped rows, expand-in-place dossier
- `apps/gitboard/src/dashboard/components/github/RepoDossier.tsx` — portal modal (now unused in gitboard; kept for beadboard kanban reuse)
- `apps/gitboard/src/dashboard/components/github/RepoContentPanels.tsx` — shared `ReadmeView`/`ChangelogView`/`ReportsView` + `parseOwnerName`
- `apps/gitboard/src/core/github-readme.ts` — GitHub Contents API fetch + 10-min cache + `parseFrontmatter` helper
- `apps/gitboard/tests/api/routes/github-releases.test.ts`
- `apps/gitboard/tests/dashboard/components/github/ReleaseTimeline.test.tsx`
- `apps/beadboard/src/dashboard/components/beads/IssueOverlay.tsx` — portal + focus trap wrapping exported `IssueDossier`

### Key modifications

**apps/gitboard/src/api/routes/github.ts** — Added three endpoints: `/releases`, `/repo/:owner/:name/markdown`, `/repo/:owner/:name/reports` (lists *.md from `.xtrm/reports/` with parsed YAML frontmatter), `/repo/:owner/:name/reports/:filename`. All hit `github-readme.ts`'s cache.

**apps/gitboard/src/core/github-store.ts** — Added `getReleases(db, filters)` that filters events table by `type='ReleaseEvent'` and maps to `GithubRelease` shape. Note: currently puts `row.title` in both `tag_name` and `name` — Codex flagged this (forge-6q1, deferred — needs schema migration).

**apps/gitboard/src/core/github-poller.ts** — One line added to capture `release.body` on insert. Existing rows backfilled via one-off script (not in code; documented in problems).

**apps/gitboard/src/dashboard/components/github/PrTimeline.tsx** — Extended `renderPrBodyText`: now parses **bold** (`**…**`), *italic* (lookbehind regex avoids italic eating bold markers), fenced code blocks (` ```lang…``` ` with lang label pill), and markdown tables (pipe-row + separator row). The function is shared with ReleaseTimeline and IssueTimeline.

**apps/gitboard/src/dashboard/components/github/GithubPanel.tsx** — TabBar now has 7 tabs (Activity, PRs, Issues, Releases, README, CHANGELOG, Reports). The last three are disabled when no repo is selected. Tab content for these three renders via `.gb-tab-pane` wrapper.

**apps/gitboard/src/dashboard/components/github/IssueTimeline.tsx** — `IssueExpandedBody` rewritten to render through `.pr-body-text` + `.pr-rich-text` (was a hand-styled div). Bodies now inherit h3/bold/list/table/fenced-code rendering.

**apps/gitboard/src/dashboard/hooks/useGithubActivity.ts** — Loads open PRs + releases on mount. Releases now sorted globally by `published_at` DESC after `flatMap` (forge-w8s, Codex fix).

**apps/gitboard/src/dashboard/styles/globals.css** — New `.gb-detail-stack` width clamp (mirrors beadboard's `.bead-expanded-stack`); `.gb-tab-pane` card surface + accent rail; `.pr-rich-text` descendant rules (h3/strong/em/code) + fenced-pre + tables; release row grid reshaped to 6 columns; symmetric padding on all expanded bodies.

**apps/beadboard/src/dashboard/components/beads/KanbanBoard.tsx** — Hoisted `selectedId`/`detail`/`loadingId` state; single overlay across columns; mounts `IssueOverlay` once.

**apps/beadboard/src/dashboard/components/beads/StatusColumn.tsx** — Slimmed: removed inline `BeadDossier`, accepts `selectedId` + `onSelect` from parent.

**apps/beadboard/src/dashboard/components/beads/IssueFeed.tsx** — Exported `IssueRow`, `IssueDossier`, `countDependencies` for reuse by Triage and Kanban. Added `IssuePrLink` interface + optional `prLink` rendering in row meta-cluster.

**apps/beadboard/src/dashboard/App.tsx** — Loads open PRs once via new `api.getOpenPrs()`. Computes `prByIssueId` map by regex-matching issue.id (escape dots) against PR title+body. Passes map through to IssueFeed / TriagePanel / KanbanBoard. TriagePanel rewritten to use IssueRow per group.

**apps/beadboard/src/dashboard/components/beads/BeadCard.tsx** — Optional `prLink` prop renders GitPullRequestIcon + #N badge.

## Documentation Updates

- 5 persistent memories saved via `bd remember`:
  - `when-briefing-executor-for-additions-to-existing-apps` — imperative IMPORT/MOUNT phrasing in CONSTRAINTS (else executors scaffold parallels)
  - `github-contents-api-get-repos-owner-repo-contents` — GH Contents API shape (base64 file, dir array, branch on Array.isArray)
  - `reuse-existing-pr-body-text-pr-rich-text` — those classes carry `white-space: pre-wrap` + browser defaults needed for markdown legibility
  - `renderprbodytext-only-handled-links-inline-code-originally-a` — regex extended for bold/italic with lookbehind to avoid eating bold
  - `markdown-table-parsing-pipe-row-separator-row-is-the-tr` — separator-row triggers table mode; parseTableCells trims pipes
- No CLAUDE.md changes; no CHANGELOG entries (dashboard-internal work).
- This session report is the only new doc artifact.

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| forge-f0g | P2 | Live GH PR/Issue updates (forge-yhm.1) | Full v3 contract filed. Cadence: 1-min for active repos (recent push OR in any client's `filter.repos`), 10-min default, 30-min idle, pause below `X-RateLimit-Remaining < 500`. Extend `GithubPoller` — do not fork. Use existing `ChannelRegistry` (broadcast on `github:activity` channel). Add `upsertPr` / `upsertIssue` to Zustand store. New WS events: `new_pr`, `new_issue`, `sync_hint` (when batch > 50). ETag cache in-memory Map (do NOT persist across restarts unless explicitly scoped). |
| forge-igg | P2 | Live Beads updates (forge-yhm.2) | Depends on forge-f0g shipping first (shared WS envelope pattern). New `BeadsChangeWatcher`: Dolt commit-hash poll (2s active project, 10s idle) primary + fs.watch on `.beads/issues.jsonl` fallback. New WS channel `beads:changes`. Source-health probe surfaces drift between Dolt and JSONL. New Zustand actions: `upsertIssue` / `moveToClosed` / `removeIssue` / `applyBatch` / `setSourceHealth`. |
| forge-6q1 | P3 | Tag_name vs name schema split | Codex P2 follow-up. Today's `getReleases` puts `row.title` into both fields. Needs schema migration: add `tag_name` column to `github_events` OR a side-table; poller stores both; `getReleases` maps separately. UI then shows short tag as mono chip + full name as title (current single-line title behavior preserved). |

### Backlog

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| forge-1fb | P3 | Beadboard: replace remaining bv parity gaps with native scoring | Spec'd as DECISION. Path (a) port pagerank into `apps/beadboard/src/dashboard/lib/graph.ts`; (b) document gap. |
| forge-che | P3 | Console: stats from observability.db | Spec'd with VERIFY-first step. First task: confirm DB path and table schema on disk. |
| forge-e23 | P3 | Console: Specialists tab — live job list + chain history | Spec'd. Shell `sp ps --json` with 2s timeout; per-turn handoff already auto-appends to bead notes so click-expand can read directly. |
| forge-yhm-related | — | — | Closed but supersedes any "live updates" backlog. |
| forge-0up | P4 | DEFERRED: Inline EventDetail in sidebar | Pre-existing deferred |
| forge-3r1 | P4 | DEFERRED: ContributionHeatmap in sidebar | Pre-existing deferred — component removed in earlier session; bead is obsolete, close on next session if confirmed stale |

## Memories Saved

| Key | Content |
|-----|---------|
| `when-briefing-executor-for-additions-to-existing-apps` | "IMPORT X from Y; do not write a replacement" imperative phrasing in CONSTRAINTS prevents executors from scaffolding parallel primitives despite reuse instructions in soft prose |
| `github-contents-api-get-repos-owner-repo-contents` | GET /repos/{owner}/{repo}/contents/{path} returns base64 file content for files OR array of entries for dirs; branch on Array.isArray(json); 404 = missing |
| `reuse-existing-pr-body-text-pr-rich-text` | Markdown bodies should use those classes — they carry white-space:pre-wrap + font/line-height baseline that browser defaults need for h3/ul/p to look right |
| `renderprbodytext-only-handled-links-inline-code-originally-a` | Extended regex pattern includes `\*\*([^*\n]+)\*\*\|(?<![*\w])\*([^*\n]+)\*(?!\*)` with negative lookbehind so italic doesn't eat bold markers |
| `markdown-table-parsing-pipe-row-separator-row-is-the-tr` | Pipe-row + separator row (\|---\|---\|) triggers table mode; reject early if next line isn't dashes-with-pipes. Fenced code: (\`\`\`\|~~~) + optional lang, consume until matching fence |

## Suggested Next Priority

1. **Merge PR #8** — All planned work landed; Codex P2 sort fix included; the tag_name schema follow-up tracked as forge-6q1. Nothing else blocks merge.
2. **forge-f0g (live GH PR/Issue push)** — Highest user-visible payoff: friend's-issue lag (today's root cause) goes away; PR badges and Issue counts update in real time. Sets up shared WS envelope for forge-igg. Contract is tight — start with `GithubPoller` extension, then store actions, then client subscription.
3. **forge-igg (live Beads push)** — After forge-f0g establishes the WS envelope pattern. Source-health probe is the riskier bit; build the Dolt commit-hash poll first and add fs.watch fallback only after that's working.
4. **forge-6q1 (tag_name vs name schema)** — Small migration; fixes the cosmetic duplication in release rows that we already saw the user comment on.
